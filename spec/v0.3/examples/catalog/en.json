{
  "_comment": "Catalogue for the practical-examples gallery at opentechevents.org/examples/. This file holds the STRUCTURE (which documents, in what order, under which filters, which fields to link) plus the English prose. Translations live next to it as <lang>.json and carry prose only. The code shown on the site is read from the documents themselves — never written here. CI fails if a document is missing from this file, if this file names a document that does not exist, or if a translation is missing: `npm run build-examples -- --check`.",

  "specVersion": "0.3.0",

  "cases": [
    { "id": "meetup", "label": "Meetups" },
    { "id": "conference", "label": "Conferences" },
    { "id": "hackathon", "label": "Hackathons" },
    { "id": "online", "label": "Online" },
    { "id": "hybrid", "label": "Hybrid" },
    { "id": "recurring", "label": "Recurring" },
    { "id": "multipart", "label": "Multi-part" },
    { "id": "co-organized", "label": "Co-organised" },
    { "id": "imported", "label": "Imported" },
    { "id": "feed", "label": "Feeds" }
  ],

  "examples": [
    {
      "file": "event-minimal.json",
      "cases": ["meetup"],
      "fields": ["event.id", "event.name", "event.startDate", "event.timezone", "event.license"],
      "title": "The smallest valid event",
      "summary": "Six lines. This is the whole barrier to entry for a small meetup — everything else is optional.",
      "points": [
        "Only `id`, `name`, `startDate` and `timezone` are required — plus `specVersion` and `license`, which a standalone document cannot inherit from a feed.",
        "`url` and `description` are strongly recommended and still not required: most published .ics files carry neither, and a spec that demanded them would force importers to drop the event or invent the data.",
        "`startDate` carries the time on the poster, with no UTC offset. `timezone` is what turns it into an unambiguous instant."
      ]
    },
    {
      "file": "event-meetup.json",
      "cases": ["meetup", "hybrid"],
      "fields": ["event.attendanceMode", "event.location", "event.location.address", "event.location.geo", "event.image", "event.tags", "event.updatedAt"],
      "title": "Hybrid meetup, with everything you'd normally want",
      "summary": "A venue and a joining URL at once, topics, coordinates, a poster — and one field that is not part of the spec yet.",
      "points": [
        "`location` and `attendanceMode` answer different questions: `location` is observable fact (is there a venue? is there a URL?), `attendanceMode` is the organiser's intent. If they disagree, `attendanceMode` wins.",
        "`venue` and `address` are the same place said twice on purpose: `venue` is the one string iCal and RSS can print, `address` is the five parts a schema.org `PostalAddress` needs — and Google validates them one by one.",
        "`updatedAt` is what lets a consumer sync incrementally — it is iCal's `LAST-MODIFIED`, not `DTSTAMP`.",
        "`image` lists the SAME poster in three crops, most useful first: several sizes of one image, not a gallery. Only the first survives iCal.",
        "`offers` is NOT in v0.3. The document is valid anyway: the schemas do not forbid extra fields, on purpose. That is how the spec grows — through fields someone already uses, which is exactly how `image` got in."
      ]
    },
    {
      "file": "event-recurring.json",
      "cases": ["meetup", "recurring"],
      "fields": ["event.partOf", "event.partOf.id", "event.status"],
      "title": "A monthly meetup: one document per occurrence",
      "summary": "This is June's edition, not the series. OTE never puts a recurrence rule in the feed — whoever publishes expands it.",
      "points": [
        "One document = one occurrence. Twelve editions are twelve documents, each with its own `id`, dates and `status`. `partOf` only says which set they belong to.",
        "That is what makes cancelling August possible: `status: \"cancelled\"` on that occurrence's own document, which stays published so subscribers find out.",
        "A consumer that ignores `partOf` still sees a complete, correct event. A recurrence rule it ignored would leave it with no dates at all.",
        "`ics:rrule` is someone else's vocabulary — the prefix says so. The importer keeps it for lossless round-trips; nobody is required to expand it."
      ]
    },
    {
      "file": "event-online.json",
      "cases": ["online", "recurring"],
      "fields": ["event.attendanceMode", "event.location.onlineUrl", "event.languages"],
      "title": "Online-only session",
      "summary": "No venue, just a joining URL — and a weekly series behind it.",
      "points": [
        "`location` carries `onlineUrl` and nothing else. An empty `location: {}` is invalid: saying nothing is already done by omitting the field.",
        "`languages` uses BCP 47 tags. Absent means unknown, never a default.",
        "Weekly sessions are published the same way monthly ones are: one document per session, grouped by `partOf`."
      ]
    },
    {
      "file": "event-hackathon.json",
      "cases": ["hackathon", "co-organized"],
      "fields": ["event.startDate", "event.endDate", "event.location.geo"],
      "title": "A hackathon that runs through the night",
      "summary": "Starts Saturday morning, ends Sunday evening, never stops. One event — and deliberately no `partOf`.",
      "points": [
        "A continuous event crossing midnight is exactly what `startDate`/`endDate` are for: both date-times, both in the same form.",
        "Compare with the multi-part study jam below. There the dates are NOT continuous, which is why it becomes several documents instead of one.",
        "Two organisers — the community and its host venue. Order matters: the first is the principal one, and the only one that survives translation to iCal."
      ]
    },
    {
      "file": "feed-multipart.json",
      "cases": ["multipart", "feed"],
      "fields": ["event.partOf.type", "event.partOf.id", "feed.events"],
      "title": "One study jam, three non-consecutive Saturdays",
      "summary": "The case that tempts everyone into stretching the dates. Three documents, one `partOf` with `type: \"multipart\"`.",
      "points": [
        "Do NOT write `startDate` on the first Saturday and `endDate` on the last: that claims a continuous fifteen-day event and blocks two full weeks in every subscriber's calendar.",
        "`type: \"multipart\"` is not decoration — it changes the translation. A series becomes schema.org `EventSeries`; a multi-part event becomes an `Event` whose parts are its `subEvent`.",
        "What `multipart` does not model is \"one single registration\". That is not a date — it is ticketing, which v0.3 leaves out. Don't fix it by deforming the time fields."
      ]
    },
    {
      "file": "event-all-day.json",
      "cases": ["conference"],
      "fields": ["event.startDate", "event.endDate", "event.timezone"],
      "title": "A two-day conference, all-day form",
      "summary": "Dates without times, when the schedule isn't the point yet.",
      "points": [
        "Both dates use the same form. Mixing (a date for `startDate`, a date-time for `endDate`) is rejected by the schema.",
        "For all-day events `timezone` contextualises the date — it says which region that day belongs to. It does not shift it, and a consumer must not convert it to another zone."
      ]
    },
    {
      "file": "event-conference-cfp.json",
      "cases": ["conference"],
      "fields": ["event.tags", "event.languages", "event.status"],
      "title": "Conference with an open call for papers",
      "summary": "The most requested thing that v0.3 still does not model — shown as the extension it currently is.",
      "points": [
        "`cfp` and `offers` are proposals, not spec. Written without a prefix, they are candidates for the core: if enough people use them, they get standardised.",
        "No generalist standard models a CFP, so on export it degrades to text or a URL.",
        "If you publish these fields, say so in the issue. Real use is the argument that moves a field into the core; a request with no case behind it is not."
      ]
    },
    {
      "file": "event-co-organized.json",
      "cases": ["co-organized", "meetup"],
      "fields": ["event.organizers", "event.organizers[].type", "event.organizers[].name"],
      "title": "Three organisers, and another project's vocabulary",
      "summary": "Two communities and a person — plus a prefixed field that belongs to a community directory, not to OTE.",
      "points": [
        "`organizers` is a list because co-organisation is the norm, not the exception. Order is significant: the first one is the principal.",
        "`type` defaults to `organization`: a translator has to pick a schema.org `@type` either way, and `Organization` is the tolerant choice.",
        "`combuilders:communityId` is not an OTE field. OTE promises never to mint a field name containing a colon, so prefixed fields cannot collide with the core — today or at 1.0."
      ]
    },
    {
      "file": "event-from-ics.json",
      "cases": ["imported", "meetup"],
      "fields": ["event.source", "event.source.license", "event.attendanceMode", "event.location.venue"],
      "title": "Imported from an .ics file",
      "summary": "What an importer can honestly produce from a calendar file — including what it must leave out.",
      "points": [
        "No `attendanceMode`, because iCalendar cannot express it. Staying silent and saying `in-person` are different claims, and only one is honest.",
        "`location.address` is missing, and the checker says so — an `.ics` carries `LOCATION` as one free-text line, with no parts to split it into. The warning names what the event lacks, not who is to blame for it.",
        "`source` is required when the event was imported or aggregated. It is orthogonal to `organizers`: who runs the event vs. where the data came from.",
        "`source.license` (what the origin allows) and `license` (what this document allows) are different fields. Declaring a licence does not grant rights the origin never gave."
      ]
    },
    {
      "file": "feed-community.json",
      "cases": ["feed", "co-organized"],
      "fields": ["feed.organizers", "feed.license", "feed.events"],
      "title": "A community feed, and how inheritance works",
      "summary": "`license` and `organizers` sit on the feed and flow down. One event overrides them — and that override replaces, it does not merge.",
      "points": [
        "Repeating `\"license\": \"CC-BY-4.0\"` in 200 events is noise, not rigour: an event that omits it inherits the feed's.",
        "An event that declares `organizers` REPLACES the inherited list. With merging there would be no way to remove an inherited organiser.",
        "That is why the second event repeats the feed's own community next to the guest one: leaving it out would attribute the event to the guest alone."
      ]
    },
    {
      "file": "feed.json",
      "cases": ["feed", "imported"],
      "fields": ["feed.title", "feed.updatedAt", "event.status"],
      "title": "An aggregator feed",
      "summary": "Someone publishing events they do not organise — the case where getting `organizers` wrong misattributes everything.",
      "points": [
        "An aggregator feed must OMIT `organizers`: it does not run what it publishes, so every event states its own.",
        "`feed.title`/`url` name whoever publishes the feed. Never fall back to them for attribution — that is exactly how an aggregator ends up credited for every event it aggregates.",
        "The cancelled event stays in the feed. Deleting it silently leaves a dead event in subscribers' calendars with no way to find out."
      ]
    }
  ]
}
