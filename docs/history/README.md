# Historical working logs

These files are preserved for context, but they are not the active change process.

- `CHANGES.log` was the Codex/Claude hand-off log used during the schema audit that led into v0.3 and v0.4.

Current spec changes go through the specification files, tests/generated docs, and `CHANGELOG.md`.
